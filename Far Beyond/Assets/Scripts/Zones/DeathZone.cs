using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class DeathZone : MonoBehaviour
{
   /* void OnTriggerEnter2D(Collider2D other) //eso nos va a permitir detectar dentro de un empty vacio con un collider y un ONTRIGGER que genere un area. 
    {
        IllayPlayer player = other.GetComponent<IllayPlayer>(); //aqui designamos que el objeto illay es el jugador. 
        if (player != null)
        {
            
            //SceneManager.LoadScene(SceneManager.GetActiveScene().name); //esto sirve para reinciar la escena EN LA QUE EL PERSONAJE DESAPAREZCA/muera.
        }
    }*/
}
