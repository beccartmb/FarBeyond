using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SaveZone : MonoBehaviour
{

    
    /*void OnTriggerEnter2D(Collider2D other) //eso nos va a permitir detectar dentro de un empty vacio con un collider y un ONTRIGGER que genere un area. 
    {
        IllayPlayer player = other.GetComponent<IllayPlayer>(); //aqui designamos que el objeto illay es el jugador. SIN ESTO NO FUNCIONA
        if (player != null)
        {
            player.transform.position = saveZone.transform.position;
        }
    }*/
}

